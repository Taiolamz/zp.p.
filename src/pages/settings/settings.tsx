import { AppContainer, CountInfo } from "../../atoms";

function Settings() {
  return (
    <AppContainer navTitle='SETTINGS'>
      <div>
        <h1>Settings</h1>
        <h1>Settings</h1>
        <h1>Settings</h1>
      </div>
    </AppContainer>
  );
}

export default Settings;
